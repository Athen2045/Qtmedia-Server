# Task 4 Fix Report

- Date: 2026-08-16
- Status: complete
- Base commit: `db4a169`
- Review findings:
  - `FaceIndex(path)` accepted existing directories until SQLite failed with `sqlite3.OperationalError`, and `refresh_images()` accepted directory-backed `image.path` values instead of rejecting them with clear `ValueError`s during validation.
  - `upsert_faces()` rewrote `images` metadata from the supplied `ImageRecord`, even though `refresh_images()` is the owner of content-hash and image-dimension metadata for the indexed row.
- Changed files:
  - `src/private_search/osint/face_store.py`
  - `tests/test_face_store.py`
  - `.superpowers/sdd/blackbird-insightface-theia-integration/task-4-report.md`
  - `.superpowers/sdd/blackbird-insightface-theia-integration/task-4-fix-report.md`
- Regression coverage:
  - Added `test_face_index_rejects_directory_database_path` in `tests/test_face_store.py`.
  - Added `test_refresh_images_rejects_directory_image_paths` in `tests/test_face_store.py`.
  - Added `test_upsert_faces_preserves_refresh_owned_image_metadata` in `tests/test_face_store.py`.
  - Red run:
    - Command: `.\\.venv\\Scripts\\python.exe -m pytest tests/test_face_store.py -q`
    - Output: `3 failed, 12 passed in 0.53s`
    - Failures showed the reviewed behavior directly:
      - `FaceIndex(directory_path)` raised `sqlite3.OperationalError: unable to open database file` instead of a validation `ValueError`.
      - `refresh_images()` accepted a directory `image.path` without raising.
      - `upsert_faces()` changed `content_hash` from `hash-refresh` to `hash-stale`.
  - Green run:
    - Command: `.\\.venv\\Scripts\\python.exe -m pytest tests/test_face_store.py -q`
    - Output: `15 passed in 0.33s`
- Ruff:
  - Command: `.\\.venv\\Scripts\\python.exe -m ruff check src/private_search/osint/face_store.py tests/test_face_store.py`
  - Output: `All checks passed!`
- Notes:
  - `FaceIndex` now rejects existing directory database targets before opening SQLite, while still allowing nested file paths whose parent directories need to be created.
  - `refresh_images()` now requires each `image.path` to resolve to an existing regular file before any image-row work begins.
  - `upsert_faces()` still replaces the face rows transactionally for an existing image row, but it now updates only `indexed_at` and `face_count`, leaving refresh-owned image metadata intact.
  - The SDD ledger was not modified.
